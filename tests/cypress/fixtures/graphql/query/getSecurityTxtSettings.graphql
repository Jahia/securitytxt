query GetSecurityTxtSettings($siteKey: String!) {
    securityTxtSettings(siteKey: $siteKey) {
        siteKey
        contact
        expires
        acknowledgments
        canonical
        encryption
        hiring
        policy
        preferredLanguages
    }
}
